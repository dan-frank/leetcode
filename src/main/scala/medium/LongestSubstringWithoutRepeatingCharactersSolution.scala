package medium

/** QUESTION: Given a string `s`, find the length of the longest substring
  * without repeating characters.
  *
  * A substring is a contiguous non-empty sequence of characters within a
  * string.
  */
object LongestSubstringWithoutRepeatingCharactersSolution {
  def apply(s: String): Int = iterative(s)

  // Iterative
  def iterative(s: String): Int = {
    ???
  }
  
  // Sliding windows?
  
  // Decreasing from max size
  
  // Bit shifting (Looking for no duplicates)?
}
